

## Plan: Update `.env` base URL

### Change

In `.env`, the `VITE_APP_BASE_URL_MINISTER` is already set to `https://execution-system.momrahai.com`. The commented-out localhost line will be removed for cleanliness.

### Files changed

| File | Change |
|---|---|
| `.env` | Remove the commented-out `# VITE_APP_BASE_URL_MINISTER=http://localhost:8235` line |

