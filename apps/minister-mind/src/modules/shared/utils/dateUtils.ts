/**
 * Re-export date utilities from @sanad-ai/ui for convenience
 * This allows apps to import from @shared/utils instead of @sanad-ai/ui
 */
export { formatDateToISO, formatDateStringToISO } from '@sanad-ai/ui';
