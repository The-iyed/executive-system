// This index file is kept for backwards compatibility.
// All calendar UI is now provided by shared components under `@shared`.
export {
  WeeklyCalendarNavigation,
  CalendarEvent,
  WeeklyCalendarGrid,
} from '@shared';
